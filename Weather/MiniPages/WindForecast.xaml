<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:res="clr-namespace:Weather.Resources"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.Maui;assembly=LiveChartsCore.SkiaSharpView.Maui"
             x:Class="Weather.MiniPages.WindForecast">
    <Grid Margin="20" BackgroundColor="LightSlateGrey">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>

        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>

        </Grid.RowDefinitions>

        <Label Grid.Row="0" HorizontalOptions="Center" FontAttributes="Bold" Text="{x:Static res:AppRes.HourlyForecast}"/>

        <lvc:CartesianChart Grid.Row="1"
                        LegendPosition="Top" Background="LightSkyBlue">
            <lvc:CartesianChart.Series>
                <lvc:SeriesCollection>
                    <lvc:XamlLineSeries
                SeriesName="{x:Static res:AppRes.CurrentWind}"
                Values="{Binding WindSpeedForecast}"/>
                </lvc:SeriesCollection>
            </lvc:CartesianChart.Series>
        </lvc:CartesianChart>

        <lvc:CartesianChart Grid.Row="2"
                        LegendPosition="Top" BackgroundColor="DeepSkyBlue">
            <lvc:CartesianChart.Series>
                <lvc:SeriesCollection>
                    <lvc:XamlLineSeries
        SeriesName="{x:Static res:AppRes.CurrentWindGust}"
        Values="{Binding WindGustForecast}"/>
                </lvc:SeriesCollection>
            </lvc:CartesianChart.Series>
        </lvc:CartesianChart>
    </Grid>
</ContentView>
